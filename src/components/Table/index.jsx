import React from 'react'
import Button from '../Button'
const Table = () => {
    return (
        <section>
            You have no Post It notes created
            <Button text={'Crear Post It'} />
        </section>
    )
}

export default Table
